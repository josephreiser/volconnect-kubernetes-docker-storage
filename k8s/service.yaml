apiVersion: v1
kind: Service
metadata:
  name: load-balancer
  labels:
    app: volconnect
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    nodePort: 30000
  selector:
    app: volconnect
